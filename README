libuseful - A collection of (mostly) useful data structures, algorithms, and utilities, including:

- hash table
- linked list
- string builder (let's you append to a cstring with automatic reallocation)
- test harness utility


